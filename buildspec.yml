version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
     - env
     - mvn -Dquarkus.lambda.handler=demo -Dlambdaname=demo clean package
     - mvn -Dquarkus.lambda.handler=dynamotest -Dlambdaname=dynamotest clean package
     - mkdir $S3_LAMBDA_PREFIX
     - mv src/target/*runner.jar $S3_LAMBDA_PREFIX
artifacts:
  files:
   - $S3_LAMBDA_PREFIX/*runner.jar
  discard-paths: no
cache:
  paths:
    - '/root/.m2/**/*'